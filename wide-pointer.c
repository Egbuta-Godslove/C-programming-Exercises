//when we delcare a pointer in C & do not intialize it then such pointer is called as "Wild pointer".


#include <stdio.h>

viod main() {

int *pointer
	printf("%d", *pointer);
return 0;
}

//In the above example *pointer is wild pointer because it do not points to anything inside the computer's memory!
